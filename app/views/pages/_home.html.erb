<div class='jumbotron center'>
	<h1>Welcome to my app!</h1>
  <% if user_signed_in? %>
    <h3>Welcome to the insiders club!</h3>
  <% else %>
		<p>
			Click here to  <%= link_to "Sign Up", new_user_registration_path,class: "btn btn-primary btn-lg" %> if you haven't!
		</p>
	<% end %>
</div>
